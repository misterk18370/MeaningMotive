MEANING MOTIVE — website update, 17 July 2026
Commit these into misterk18370/MeaningMotive, preserving structure:

  index.html                 -> repo ROOT (replaces the current homepage)
  keeping-map/index.html     -> NEW folder /keeping-map/  (creates /keeping-map/)

That is the whole update. Both files are self-contained, use the site's
existing style.css and design tokens, load no web fonts, run no scripts,
and set no cookies. The homepage gains an animated Keeping Map centrepiece
and a "Keeping Map" nav entry; /keeping-map/ is the full journey page.

You can also delete the earlier stray upload mm_keeping_map_v0_3_170726.html
from the repo root — it is superseded by /keeping-map/.

STILL TO DO (I can hand you these on request): the other pages
(story, experiment, evidence, clause, community, colophon, for-ai) do not
yet carry the "Keeping Map" nav link, so the top nav differs on those pages.
Say the word and I will return each with the one nav line added.
